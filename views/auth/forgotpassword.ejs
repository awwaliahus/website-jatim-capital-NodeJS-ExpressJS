<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>

  <!-- ✅ Tambahkan Google Fonts: Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Poppins', sans-serif; /* ✅ Font Poppins digunakan */
      background-color: #f5f5f5;
    }

    .container {
      display: flex;
      height: 100vh;
    }

    .left {
      flex: 1;
      background-color: #ffffff;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px;
    }

    .form-box {
      width: 100%;
      max-width: 400px;
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: 600;
      color: #111827;
    }

    label {
      display: block;
      margin: 15px 0 5px;
      color: #374151;
      font-size: 14px;
    }

    input[type="email"] {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      font-size: 14px;
      font-family: 'Poppins', sans-serif; /* ✅ Tambahkan agar input juga pakai Poppins */
    }

    .btn-reset {
      width: 100%;
      background-color: #000000;
      color: #ffffff;
      padding: 12px;
      margin-top: 25px;
      font-weight: 500;
      font-size: 14px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
      font-family: 'Poppins', sans-serif;
    }

    .btn-reset:hover {
      background-color: #111827;
    }

    .image-right {
      flex: 1;
      display: flex;
      align-items: flex-start;
      justify-content: flex-end;
      background-color: #ffffff;
      height: 100vh;
      overflow: hidden;
    }

    .image-right img {
      height: 100vh;
      width: auto;
      object-fit: contain;
    }

    .nav-top {
      position: absolute;
      top: 20px;
      right: 40px;
      font-family: 'Poppins', sans-serif; /* ✅ Pastikan navigasi juga pakai Poppins */
    }

    .nav-top a {
      margin-left: 10px;
      padding: 8px 14px;
      text-decoration: none;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 6px;
      color: #111;
      background: #fff;
      transition: background 0.3s;
    }

    .nav-top a:hover {
      background: #f3f3f3;
    }

    .nav-top a.active {
      background: #111;
      color: #fff;
    }
  </style>
</head>

<body>
  <div class="nav-top">
    <a href="/login">Log In</a>
    <a href="/signup">Sign Up</a>
  </div>

  <div class="container">
    <div class="left">
      <div class="form-box">
        <h2>Forgot your password?</h2>
        <p style="font-size: 14px; color: #555;">Enter your email address and we’ll send you a link to reset your password.</p>
        <form action="/forgot-password" method="POST">
          <label for="email">Email address</label>
          <input type="email" name="email" id="email" placeholder="you@example.com" required>
          <button type="submit" class="btn-reset">Send Reset Link</button>
        </form>
      </div>
    </div>

    <div class="image-right">
      <img src="/img/frame-427319048-9.png" alt="Forgot Password Illustration">
    </div>
  </div>
</body>

</html>
